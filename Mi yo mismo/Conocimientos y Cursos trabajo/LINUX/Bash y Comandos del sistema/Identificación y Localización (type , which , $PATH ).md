- **`type`**: Indica si un comando es interno o un fichero externo (ej. `type cd`).
    
- **`which`**: Muestra la ruta absoluta del programa ejecutable (ej. `which ls`).
    
- **Variable `$PATH`**: Define los directorios donde BASH busca los ejecutables. Si un programa no está en el `$PATH`, debe ejecutarse indicando la ruta, como `./programa` para el directorio actual.